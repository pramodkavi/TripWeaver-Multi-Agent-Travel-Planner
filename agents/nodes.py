"""LangGraph node functions: intent router + General QA, Hotel, Flight agents.

To be implemented in Phase 2 (SRS E2) and Phase 3 (SRS E3, graceful
degradation). Agents select and call MCP tools only, never fabricate data,
and ask follow-up questions for missing inputs.
"""
